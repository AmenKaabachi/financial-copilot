# Reporting Shell & Dashboard Fixes

## Steps

- [x] 1. Fix tab centering in `reporting-shell.component.html` — replace `[class]` with `ngClass`
- [x] 2. Fix dashboard padding — change `px-0` to `px-4` on main container
- [x] 3. Fix dashboard empty state — remove emoji, heading, text, and duplicate button
- [x] 4. Fix dashboard error state — remove or simplify "Try again" button
- [x] 5. Verify all changes work correctly
